import React from 'react'
import styles from './index.module.css'

export const Header = () => {
  return (
    <header className={`py-5 ${styles.header}`}>
        <h1>Header</h1>
        
    </header>
  )
}
